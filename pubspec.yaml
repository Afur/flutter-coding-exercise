name: spa_coding_exercise
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.24.1
  http: ^0.12.2
  flutter_bloc: ^6.0.0
  freezed_annotation: ^0.11.0+1
  google_fonts: ^1.1.0
  geolocator: ^6.1.13
  dartz: ^0.8.9
  get_it: ^4.0.4
  injectable: ^0.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.8.0
  freezed: ^0.11.5
  injectable_generator: ^0.3.0

flutter:
  uses-material-design: true

